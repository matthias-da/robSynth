% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{utility}
\alias{utility}
\title{Utility assessment for synthetic data}
\usage{
utility(object, original = NULL, metrics = NULL, ...)
}
\arguments{
\item{object}{a \code{"robsynth"} object, or a synthetic data.frame}

\item{original}{the original data.frame (required if \code{object}
is a data.frame, optional if \code{object} is a robsynth object
with \code{$original} stored)}

\item{metrics}{character vector of metrics to compute.  Options:
\code{"pMSE"}, \code{"KS"}, \code{"regression"}, \code{"correlation"},
\code{"overlap"}.  Default: all.}

\item{...}{additional arguments passed to metric functions}
}
\value{
An S3 object of class \code{"robsynth_utility"} with
  components for each metric and a formatted print method.
}
\description{
Unified interface to compute all utility metrics comparing original
and synthetic data.  Handles multiple synthetic copies (\code{m > 1})
by averaging across copies.
}
\examples{
\dontrun{
data(CrohnD, package = "robustbase")
dat <- CrohnD[, -1]
res <- robsynth(dat)
utility(res, dat)
}
}
