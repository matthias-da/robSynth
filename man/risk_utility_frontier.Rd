% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{risk_utility_frontier}
\alias{risk_utility_frontier}
\title{Risk-utility frontier analysis}
\usage{
risk_utility_frontier(
  orig,
  synth_list,
  utility_metric = "pMSE",
  risk_metric = "distance",
  key_vars = NULL,
  sensitive_var = NULL
)
}
\arguments{
\item{orig}{original data.frame}

\item{synth_list}{named list of synthetic data.frames to compare}

\item{utility_metric}{which utility metric to use as scalar.
Default: \code{"pMSE"}.}

\item{risk_metric}{which risk metric to use as scalar.
Default: \code{"distance"}.}

\item{key_vars}{passed to \code{\link{synth_risk}}}

\item{sensitive_var}{passed to \code{\link{synth_risk}}}
}
\value{
A data.frame of class \code{"ru_frontier"} with columns:
  \code{method}, \code{utility}, \code{risk}, \code{pareto_optimal}.
}
\description{
Compares multiple synthetic datasets on the risk-utility frontier
(Duncan-Lambert R-U confidentiality map). Each synthesizer produces
a point in (risk, utility) space; the frontier shows which methods
are Pareto-optimal.
}
\examples{
\dontrun{
data(CrohnD, package = "robustbase")
dat  <- CrohnD[, -1]
cont <- c("BMI", "height", "age", "weight")
s1   <- dat[sample(nrow(dat), replace = TRUE), ]
s2   <- dat
s2[, cont] <- as.matrix(s2[, cont]) +
             matrix(rnorm(nrow(dat) * length(cont), sd = 0.5),
                    ncol = length(cont))
res <- risk_utility_frontier(dat, list(bootstrap = s1, noisy = s2))
plot(res$utility, res$risk, pch = 19)
text(res$utility, res$risk, res$method, pos = 3)
}
}
\author{
Matthias Templ
}
