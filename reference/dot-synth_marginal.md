# Marginal synthesis for a single variable

Marginal synthesis for a single variable

## Usage

``` r
.synth_marginal(y, type, n, w = NULL)
```

## Arguments

- y:

  vector of original values

- type:

  `"continuous"` or `"categorical"`

- n:

  number of synthetic values

## Value

vector of length `n`
