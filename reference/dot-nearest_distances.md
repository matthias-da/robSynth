# Distance from each row of `X_query` to its nearest row in `X_ref`, in Euclidean metric. Expects both inputs on the same scale.

Distance from each row of `X_query` to its nearest row in `X_ref`, in
Euclidean metric. Expects both inputs on the same scale.

## Usage

``` r
.nearest_distances(X_query, X_ref)
```
