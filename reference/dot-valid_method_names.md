# Known synthesis method names (including \`"passive"\` and \`""\`, which are handled outside the registry in \`robsynth()\`).

Known synthesis method names (including \`"passive"\` and \`""\`, which
are handled outside the registry in \`robsynth()\`).

## Usage

``` r
.valid_method_names()
```
